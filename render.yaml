# Render Blueprint for Building Node Backend

build:
  commands:
    - cd Downloads/lead-scoring-pipeline/backend
    - npm install  # or npm ci

start:
  commands:
    - cd Downloads/lead-scoring-pipeline/backend
    - npm start
